# API URL for the backend
# For development: VITE_API_URL=http://localhost:8000
# For production: Leave empty to use the same origin as the frontend (recommended)
# This allows the app to work with any domain/IP without rebuilding
VITE_API_URL=http://localhost:8000
